// Your first task is to write a function that will take two string parameters 
// and return a string containing the sender name and the message in the form specified in the instructions.


const parseMessage = (origin, message) => {
  // Code here!

  // Want to see what's going on inside your function? 
  // You can use console.log() to debug and print variables to the console below.
  
  // Remember to return a value!
  return `${origin}: ${message}`
}
